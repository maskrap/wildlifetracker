<h1><%= @species.name %></h1>

<h2>Sightings:</h2>
<% if @species.sightings.any? %>
  <ul>
    <li>
      <% @species.sightings.each do | sighting | %>
        <%= sighting.date %>
        <%= sighting.longitude %>
        <%= sighting.latitude %>
        <%= sighting.regions.name %>
        <%= link_to "Edit", edit_species_sighting_path(@species, sighting) %> |
        <%= link_to "Delete", species_sighting_path(@species, sighting),
                              :data => {:confirm => "Are you sure?",
                                          :method => "delete"} %>
      <% end %>
    </li>
  </ul>
<% else %>
  <p>The forest is gone</p>
<% end %>

<p><%= link_to "Add a new Sighting", new_species_sighting_path(@species) %></p>
<p><%= link_to "Delete Species", species_path(@species),
                                  :data => {:confirm => "Are you really sure?",
                                            :method => "delete"}%></p>
<p><%= link_to "Return to Species List", species_path %></p>
